fun main() {
    // val type variables are immutable references
    val apple = false;

    // var type variables ar mutable references
    var sum = 0;
    sum = 32;

    println(sum)
    println(apple)
}